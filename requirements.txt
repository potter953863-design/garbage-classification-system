# PyTorch with CUDA support (install via: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124)
# For CPU-only version, use: pip install torch torchvision
torch>=2.0.0
torchvision>=0.15.0
streamlit>=1.28.0
matplotlib>=3.7.0
numpy>=1.24.0
Pillow>=10.0.0
scikit-learn>=1.3.0
pandas>=2.0.0
seaborn>=0.12.0
tqdm>=4.65.0
# SQLite is included in Python standard library, no need to install separately

# Map and location services
folium>=0.14.0
streamlit-folium>=0.15.0
requests>=2.31.0

